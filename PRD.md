# 远望书友（YuanWangBookFriend.koplugin）产品需求文档

- **版本**：V1.0（草案）
- **日期**：2026-09-19
- **文档状态**：待评审
- **产品定位**：KOReader AI 辅助阅读插件 —— 把大模型变成随时在场的"书友"，不打断阅读，只在你需要时出现。

---

## 1. 项目概述

### 1.1 背景

墨水屏深度阅读存在三个长期痛点：遇到不懂的概念没人问、隔几天续读忘了前文、长篇作品人物关系理不清。云端大模型（DeepSeek）恰好能解决这三件事，但 Kindle/安卓阅读器上缺乏一个"轻量、防剧透、低打扰"的 AI 接入方案。

远望书友是一款 KOReader 插件，以"选中即问、异步轻答、严格防剧透"为核心交互，把 DeepSeek 的能力嵌入阅读全流程。

### 1.2 目标平台与环境

| 项目 | 要求 |
|---|---|
| 阅读设备 | KPW6、KPW4（越狱 Kindle）、掌阅 neo3ultra（安卓） |
| KOReader 版本 | 2026.07 及以上 |
| 插件英文名 | `YuanWangBookFriend.koplugin` |
| 插件中文名 | 远望书友 |
| AI 服务 | DeepSeek API（暂只接入此一家），API Key 由用户提供 |
| 开发语言 | Lua（KOReader 插件标准） |

### 1.3 核心设计原则

1. **安装零污染**：所有文件（代码、配置、缓存、密钥）均限制在插件自身目录 `koreader/plugins/YuanWangBookFriend.koplugin/` 内，卸载即删目录，不留残迹。
2. **阅读优先**：AI 永远不打断阅读流。默认轻交互，深交互由用户主动发起。
3. **防剧透是底线**：AI 对未读内容在工程上不可见，而非"提示词约定"。
4. **低资源占用**：KPW4 级别的设备（约 512MB 内存、单核）也能流畅运行。
5. **Token 节俭**：能离线缓存的绝不重复请求，能截断的上下文绝不全量发送。

---

## 2. 用户与使用场景

### 2.1 目标用户

- 使用越狱 Kindle（KPW4/KPW6）+ KOReader 的深度阅读者
- 安卓墨水屏设备（掌阅 neo3ultra 等）用户
- 典型画像：阅读长篇虚构/非虚构作品，愿意折腾设备但重视隐私，对 AI 辅助持开放态度

### 2.2 典型使用场景

| 场景 | 用户行为 | 期望结果 |
|---|---|---|
| 场景 A：偶遇生僻概念 | 阅读中选中"康乾盛世"，长按菜单点"解释" | 结合上下文给出历史背景解释，30 秒内返回 |
| 场景 B：隔周续读 | 打开一周没碰的《百年孤独》 | 一键生成"剧情回顾"，快速找回记忆 |
| 场景 C：深度讨论 | 读完一章后想聊聊人物动机 | 进入全屏深聊模式，与 AI 多轮对话 |
| 场景 D：防剧透焦虑 | 问"这个人后来死了吗" | 若涉及未读内容，返回模糊预警而非答案 |
| 场景 E：人物太多记不清 | 选中一个似曾相识的人名 | 自动匹配 X-Ray 词条，展示该人物档案与关系 |

---

## 3. 功能需求详述

> 需求优先级：**P0** = 首版必须交付；**P1** = 首版尽量交付，可砍；**P2** = 后续迭代。

### 3.1 模块一：AI 对话与问答

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F1.1 | 深聊模式 | 选中文字后进入**全屏对话界面**，以选中内容为引子，支持多轮对话，适合深入讨论人物、情节、主题 | P0 |
| F1.2 | 轻问模式 | 选中文字后**底部弹出消息卡片**提交问题，用户继续阅读，回复异步送达（通知角标提示），点击卡片查看 | P0 |
| F1.3 | 全局 AI 助手 | 通过主菜单入口或自定义手势随时呼出对话窗口，支持自由提问（不依赖选中文本） | P0 |
| F1.4 | 模式切换 | 深聊/轻问界面可一键互相切换；轻问卡片可"展开为深聊" | P1 |

**交互细节要求：**
- 深聊模式界面必须适配 E-ink：高对比度、无渐变、局部刷新优先，避免整屏闪烁。
- 轻问卡片默认高度不超过屏幕 1/4，3 秒无操作自动缩小为角标，不遮挡正文超过必要时间。
- 多轮对话默认携带最近 3 轮上下文，超出部分自动摘要压缩（见 4.3 token 控制策略）。

### 3.2 模块二：AI 阅读理解辅助

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F2.1 | 上下文智能释义 | 选中词汇/短语/指代物，AI 结合**当前段落与前后文**给出语境化解释（而非词典定义） | P0 |
| F2.2 | 段落/章节摘要 | 对选中段落一键概括；对当前章节一键生成摘要 | P0 |
| F2.3 | 概念解释 | 对专业术语、历史事件、文化典故补充背景知识卡片 | P0 |
| F2.4 | 作者介绍 | 基于当前书籍元数据，生成作者生平、创作背景、相关作品介绍 | P1 |

**输入边界**：F2.1/F2.2 的上下文窗口默认取选中位置前后各约 800 字符（可配置 400–2000），并在防剧透模式开启时只取当前位置**之前**的内容（见模块四）。

### 3.3 模块三：X-Ray 人物与情节管理

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F3.1 | AI 生成 X-Ray 词条 | 用户触发"分析本书"后，AI 分章节抽取人物、地点、组织、事件，生成词条卡片（名称、别名、首次出现、简介、相关章节） | P0 |
| F3.2 | 词条卡片展示 | 选中文字命中词条时，弹出该词条卡片；含出场章节列表，点击可跳转 | P0 |
| F3.3 | 词条模糊匹配 | 选中文字有偏差（截断、多选、错字）时，按编辑距离+语义别名匹配最接近词条，并显示置信度 | P0 |
| F3.4 | 剧情回顾 | 距上次打开本书超过 N 天（默认 3 天，可配置）时，阅读器弹出提醒，可一键生成"截至当前进度"的剧情回顾 | P1 |
| F3.5 | 人物关系图谱 | 可视化展示角色关系（节点+边标签）。E-ink 上以静态灰度图渲染，安卓端允许简单缩放 | P1 |
| F3.6 | 故事时间线 | 对非线性叙事作品，AI 梳理"故事内时间"与"叙事顺序"双线时间轴 | P2 |

**关键技术约束：**
- 词条生成采用**增量分析**：只分析用户新读过的章节，不重复处理全书，控制 token 成本。全书一次性分析作为可选操作并明确提示预估消耗。
- 词条数据本地 SQLite（或 JSON）持久化，随书籍指纹（文件 hash）关联，书籍文件变更时提示重新分析。
- F3.4/F3.5/F3.6 严格受防剧透约束：只基于已读内容生成。

### 3.4 模块四：防剧透保护（核心差异化功能）

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F4.1 | 防剧透全局开关 | 设置页总开关，默认**开启** | P0 |
| F4.2 | 进度感知回答 | AI 请求发生时实时读取 KOReader 当前阅读位置，上下文只截取已读范围 | P0 |
| F4.3 | 章节内容隔离（工程级） | 未读章节文本**在插件侧物理截断**，不进入任何发往 AI 的 payload。不是靠 system prompt 嘱咐模型"别剧透" | P0 |
| F4.4 | 剧透预警提示 | 当 AI 回答或用户问题被判定涉及未读内容时，返回标准化模糊提示："这部分内容在后续章节中有所揭示，建议继续阅读。" | P0 |
| F4.5 | 防剧透粒度设置 | 支持两种粒度：按章节边界 / 按全书百分比（当前进度±0%），用户可选 | P1 |

**实现要点：**
- 阅读进度来源：KOReader 的 `ReaderRolling`/`ReaderPaging` 位置接口，换算为全书字符偏移或章节序号。
- 双保险：①发送前在插件侧按进度截断文本；②system prompt 中注入当前进度声明与"禁止引用截断点之后内容"指令。
- 预警判定：轻量做法——AI 返回后由本地关键词/章节引用检测兜底；不额外消耗一次 API 调用做判定（P1 可加入可选的二次校验）。

### 3.5 模块五：书库 AI 分析

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F5.1 | 书库智能分类 | 扫描书库元数据（书名、作者、简介、标签），AI 按主题/风格聚类，生成可浏览的分类视图 | P1 |
| F5.2 | 阅读推荐 | 基于已读书单与阅读统计，推荐同类型/延伸书目（书单形式，不含购买链接） | P1 |
| F5.3 | 阅读统计报告 | 周期性（周/月）生成阅读洞察：时长、进度、偏好变化、简评 | P2 |

**约束**：F5.1 默认只发送书籍元数据，不发送书籍正文（正文分析需逐书单独触发，明示 token 消耗）。

### 3.6 模块六：手势与交互

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F6.1 | 自定义手势 | 配置单指滑动（上/下/左/右/对角）、双指操作、长按（屏幕区域）触发指定 AI 功能（呼出全局助手、轻问选中文本、剧情回顾等） | P1 |
| F6.2 | 快捷操作菜单 | KOReader 长按文本的上下文菜单中注入 AI 入口：解释 / 轻问 / 深聊 / X-Ray 查询 | P0 |

**实现路径**：手势通过 KOReader `GestureManager` API 注册；菜单注入通过 KOReader 的高亮菜单扩展点。不 hook 私有接口，保证 2026.07+ 版本兼容。

### 3.7 模块七：设备适配与性能优化

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F7.1 | E-ink UI 优化 | 高对比度配色（纯黑文字/纯白底）、禁用阴影渐变、对话流采用局部刷新，输入框聚焦时才全刷 | P0 |
| F7.2 | KPW4 精简模式 | 检测到内存 < 768MB 或设备型号为 KPW4 时自动启用：关闭动画、降低对话历史加载量、简化渲染（关系图谱降级为列表） | P0 |
| F7.3 | 回复离线缓存 | 所有 AI 回复本地持久化（按书籍+选中内容 hash 索引），重复提问命中缓存直接返回，不消耗 token；断网可浏览全部历史 | P0 |
| F7.4 | 请求队列管理 | AI 请求串行队列（并发数=1，可配置到 2），带超时（默认 60s）、重试（最多 2 次，指数退避）、失败降级提示 | P0 |
| F7.5 | 后台异步执行 | 网络请求不阻塞阅读主线程；KPW4 上限制插件总内存占用 < 40MB | P0 |

### 3.8 模块八：隐私与数据安全

| 编号 | 需求 | 描述 | 优先级 |
|---|---|---|---|
| F8.1 | 数据本地化 | 阅读进度、笔记、高亮、对话记录、X-Ray 数据全部仅存设备本地插件目录 | P0 |
| F8.2 | 按需上传 | 仅在用户主动触发 AI 功能时发送数据，且发送前在 UI 上可预览将发送的内容范围（P1：预览面板） | P0 |
| F8.3 | 对话记录管理 | 支持按书籍查看、全文搜索、单条删除、批量删除、全部清空 | P0 |
| F8.4 | API Key 加密存储 | Key 不以明文落盘；使用设备唯一标识派生密钥做对称加密（如 AES，通过 KOReader 可用的 Lua 加密库或 openssl 命令行）；设置页显示为掩码 | P0 |
| F8.5 | 无遥测 | 插件不含任何统计上报、崩溃上报、第三方分析代码 | P0 |
| F8.6 | 网络白名单 | 插件仅连接 `api.deepseek.com`，设置页明示此事实 | P1 |

---

## 4. 非功能需求

### 4.1 性能指标

| 指标 | 目标值 |
|---|---|
| 插件冷启动对 KOReader 启动时间影响 | < 300ms（KPW4） |
| 轻问请求从提交到卡片消失（不等待回复） | < 500ms |
| 插件常驻内存（空闲） | < 20MB（KPW4）/ < 40MB（峰值） |
| 缓存查询响应 | < 200ms |
| 全书 X-Ray 分析（30 万字小说） | 增量模式下用户无感知；全书模式预估 token 明示 |

### 4.2 兼容性

- KOReader 2026.07+（只使用公开 API，版本检测失败时优雅禁用并提示）
- KPW6 / KPW4 / 掌阅 neo3ultra 屏幕分辨率与 DPI 自适应
- 设备无网络时全部本地功能（历史、缓存、X-Ray 浏览）可用

### 4.3 Token 成本控制策略

1. **上下文截断**：默认前后各 800 字符，上限 2000，超长自动摘要。
2. **多轮压缩**：对话超过 3 轮后，历史消息由 AI 摘要为一段上下文。
3. **缓存优先**：相同书籍+相同选中内容+相同问题类型，直接命中本地缓存。
4. **增量分析**：X-Ray 只处理新读章节。
5. **用量透明**：设置页展示累计 token 消耗与预估费用（按 DeepSeek 公开价目）。

### 4.4 安装与卸载

- **安装**：将 `YuanWangBookFriend.koplugin` 文件夹拷入 `koreader/plugins/`，重启 KOReader 即可。
- **卸载**：删除该文件夹。无任何外部文件、注册项、KOReader 配置侵入（设置存于插件目录内 `settings.json`，不写入 KOReader 全局设置）。
- **首次运行引导**：引导页引导用户粘贴 DeepSeek API Key（加密保存），并说明防剧透机制与隐私承诺。

---

## 5. 技术方案概要

### 5.1 架构分层

```
┌─────────────────────────────────────────┐
│ UI 层（深聊界面 / 轻问卡片 / X-Ray 卡片 / 设置页）│  E-ink 优化渲染
├─────────────────────────────────────────┤
│ 功能层（对话 / 释义 / 摘要 / X-Ray / 书库分析）   │
├─────────────────────────────────────────┤
│ 核心服务层                                │
│  ├─ 防剧透引擎（进度读取 / 文本截断）        │
│  ├─ 请求队列（串行 / 重试 / 超时）           │
│  ├─ 缓存服务（hash 索引 / 持久化）           │
│  └─ Prompt 构建器（模板 + 上下文组装）       │
├─────────────────────────────────────────┤
│ 基础设施层                                │
│  ├─ DeepSeek HTTP 客户端（仅 api.deepseek.com）│
│  ├─ 本地存储（SQLite / JSON）               │
│  └─ 加密模块（API Key 保护）                │
└─────────────────────────────────────────┘
```

### 5.2 关键技术决策

| 决策点 | 方案 | 理由 |
|---|---|---|
| 网络请求 | KOReader 内置 `socket.http`/`ltn12` 异步封装 | 无外部依赖，兼容 Kindle/安卓 |
| 本地存储 | SQLite（KOReader 自带）存对话/X-Ray；JSON 存设置 | 查询性能与便携性兼顾 |
| 文本提取 | KOReader `document` API 按位置范围取纯文本 | 支持 EPUB/MOBI/PDF（PDF 视 OCR 情况降级） |
| 加密 | Lua 可用的 AES 实现或调用系统 openssl，密钥派生自设备序列号 hash | 满足"不明文落盘"即可，不追求抗物理攻击 |
| 图谱渲染 | Lua 计算布局 + KOReader `ImageWidget` 静态渲染 | E-ink 无 GPU，避免实时绘制 |

### 5.3 DeepSeek API 使用

- 端点：`https://api.deepseek.com/chat/completions`
- 模型：默认 `deepseek-chat`（对话/释义/摘要），可在设置中切换 `deepseek-reasoner`（深聊模式可选，提示用户成本更高）
- 参数基线：`temperature=0.7`（对话）/ `0.3`（释义、摘要、X-Ray 抽取）；`max_tokens` 按功能类型预设（释义 512 / 摘要 1024 / 深聊 2048）
- API Key 来源：用户自行配置；开发测试用 Key 存放于项目根目录 `DeepSeek_API_KEY.txt`（该文件仅用于开发，不打入插件包）

---

## 6. 里程碑规划

| 阶段 | 范围 | 交付物 |
|---|---|---|
| M1 骨架 | 插件框架、设置页、API Key 加密存储、DeepSeek 连通、请求队列 | 可发问并收到回复的最小插件 |
| M2 核心问答 | 深聊/轻问双模式、上下文释义、段落摘要、全局助手、缓存 | 完整问答体验 |
| M3 防剧透 | 进度读取、文本截断、预警提示、粒度设置 | 防剧透全链路 |
| M4 X-Ray | 词条生成、卡片、模糊匹配、剧情回顾 | X-Ray 基础闭环 |
| M5 增强 | 关系图谱、手势自定义、书库分类、KPW4 精简模式打磨 | V1.0 发布候选 |

每个里程碑必须在 KPW4 真机验证一次（性能红线：见 4.1）。

---

## 7. 风险与开放问题

| 风险/问题 | 影响 | 应对 |
|---|---|---|
| KOReader 2026.07 部分 API 在 Kindle 与安卓端行为差异 | 中 | M1 即搭双端真机测试；差异点封装适配层 |
| DeepSeek API 在 Kindle 上的 TLS/证书兼容性 | 高 | M1 第一项验证任务；必要时内置 CA bundle |
| KPW4 内存紧张时 KOReader 整体 OOM | 高 | 精简模式 + 内存红线监控 + 队列串行化 |
| 防剧透的"已读"定义：用户跳读/翻目录浏览后进度如何认定 | 中 | 以"当前阅读位置"为准，另提供手动标记已读范围的入口（P1） |
| 全书 X-Ray 分析 token 成本超预期 | 中 | 默认增量模式；全书模式前置确认 + 成本预估 |
| PDF 重排文本质量差导致 AI 理解偏差 | 低 | PDF 文档降级提示，优先支持 EPUB/MOBI |
| 多轮对话隐私：上下文累积发送范围扩大 | 低 | 对话上下文仅限当前书籍已读范围，遵循同一防剧透管道 |

---

## 8. 验收标准（V1.0 摘要）

1. 在 KPW6、KPW4、掌阅 neo3ultra 三台设备上完成安装、配置 Key、发起问答全流程，无崩溃。
2. 防剧透开启时，构造"询问未读章节情节"的用例，插件发往 DeepSeek 的 payload 中不含未读章节文本（抓包/日志验证）。
3. 删除插件目录后，KOReader 其余功能与数据无任何变化。
4. KPW4 上连续发起 5 个请求，UI 无卡死，插件峰值内存 < 40MB。
5. 断网状态下可浏览全部历史对话与 X-Ray 词条。
6. 相同问题第二次提问零 API 调用（缓存命中）。
